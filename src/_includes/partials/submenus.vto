<ul class="nav-list">
  {{ for menu of site.galleries }}
    {{ if menu.url === page.url }}
      <li class="inactive">{{ menu.name }}</li>
    {{ else }}
      <li>
        <a href="{{ menu.url }}">{{ menu.name }}</a>
      </li>
    {{ /if }}
  {{ /for }}
</ul>
